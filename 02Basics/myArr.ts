const superHeros: number[] = [];
const superHeroPow: Array<number> = [];

superHeros.push(1);

type User = {
  name: string;
  isActive: boolean;
};

const allUser: User[] = [
  /* all that will go here must be of that syntax*/
];

const MLModels: number[][] = [
  /* array of array*/
];
